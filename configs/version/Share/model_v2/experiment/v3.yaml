# lightning.pytorch==2.0.7
trainer:
  max_epochs: 10 #!!!
  log_every_n_steps: 1 #!!!
data:
  init_args:
    train_datamodules:
    - class_path: src.dataset.ffpp.FFPPDataModule
      init_args:
        augmentations:
        - DEV
        - RRC